import { SearchItemPipe } from './search-item.pipe';

describe('SearchItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchItemPipe();
    expect(pipe).toBeTruthy();
  });
});
